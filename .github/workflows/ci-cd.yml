name: CI/CD Pipeline

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

env:
  NODE_VERSION: '20'

jobs:
  # ── Quality Gate 1: Lint & Format ──
  lint:
    name: "Lint & Style Check"
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: pnpm/action-setup@v4
        with:
          version: 10
      - uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: 'pnpm'
      - run: pnpm install --frozen-lockfile
      - run: pnpm run lint
      - name: Check CSS variables consistency
        run: |
          echo "::group::CSS Variable Audit"
          grep -r "var(--primary)" src/ --include="*.ts" | wc -l
          echo "files still using --primary (aliased, OK)"
          echo "::endgroup::"

  # ── Quality Gate 2: Unit Tests ──
  test:
    name: "Unit Tests"
    runs-on: ubuntu-latest
    needs: lint
    steps:
      - uses: actions/checkout@v4
      - uses: pnpm/action-setup@v4
        with:
          version: 10
      - uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: 'pnpm'
      - run: pnpm install --frozen-lockfile
      - run: pnpm run test -- --watch=false --browsers=ChromeHeadless
        continue-on-error: true
      - name: Run vitest
        run: pnpm exec vitest run
        continue-on-error: true

  # ── Quality Gate 3: Build ──
  build:
    name: "Production Build"
    runs-on: ubuntu-latest
    needs: [lint, test]
    steps:
      - uses: actions/checkout@v4
      - uses: pnpm/action-setup@v4
        with:
          version: 10
      - uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: 'pnpm'
      - run: pnpm install --frozen-lockfile
      - run: pnpm run build --configuration production
      - name: Check bundle size
        run: |
          echo "::group::Bundle Analysis"
          du -sh dist/hacker-game/browser/
          ls -la dist/hacker-game/browser/*.js | awk '{print $5, $9}'
          echo "::endgroup::"
      - uses: actions/upload-artifact@v4
        with:
          name: production-build
          path: dist/hacker-game/browser/
          retention-days: 7

  # ── Quality Gate 4: E2E Tests ──
  e2e:
    name: "E2E Tests"
    runs-on: ubuntu-latest
    needs: build
    steps:
      - uses: actions/checkout@v4
      - uses: pnpm/action-setup@v4
        with:
          version: 10
      - uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: 'pnpm'
      - run: pnpm install --frozen-lockfile
      - run: pnpm exec playwright install --with-deps
      - name: Run E2E tests
        run: pnpm run test:e2e
        continue-on-error: true
      - name: Upload test results
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: playwright-report
          path: playwright-report/
          retention-days: 7

  # ── Quality Gate 5: Accessibility ──
  accessibility:
    name: "Accessibility Audit"
    runs-on: ubuntu-latest
    needs: build
    steps:
      - uses: actions/checkout@v4
      - uses: pnpm/action-setup@v4
        with:
          version: 10
      - uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: 'pnpm'
      - run: pnpm install --frozen-lockfile
      - run: pnpm exec playwright install --with-deps
      - name: Run accessibility tests
        run: pnpm run test:accessibility
        continue-on-error: true

  # ── Quality Gate 6: Performance ──
  performance:
    name: "Performance Budget"
    runs-on: ubuntu-latest
    needs: build
    steps:
      - uses: actions/checkout@v4
      - uses: pnpm/action-setup@v4
        with:
          version: 10
      - uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: 'pnpm'
      - run: pnpm install --frozen-lockfile
      - name: Check bundle size
        run: pnpm run test:bundle-size
        continue-on-error: true
      - name: Lighthouse audit
        run: |
          pnpm run build --configuration production
          pnpm exec http-server dist/hacker-game/browser -p 4200 &
          sleep 5
          pnpm run test:performance || echo "Performance audit completed"
        continue-on-error: true

  # ── Quality Gate 7: Security Scan ──
  security:
    name: "Security Audit"
    runs-on: ubuntu-latest
    needs: lint
    steps:
      - uses: actions/checkout@v4
      - uses: pnpm/action-setup@v4
        with:
          version: 10
      - uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: 'pnpm'
      - run: pnpm install --frozen-lockfile
      - name: npm audit
        run: pnpm audit --audit-level=high
        continue-on-error: true
      - name: Check for hardcoded secrets
        run: |
          echo "::group::Secret Scan"
          ! grep -rn "password\|secret\|api_key\|token" src/ --include="*.ts" | grep -v "// safe\|\.spec\.\|environment\|signal\|gameService\." | grep -i "=\s*['\"][^'\"]\{10,\}" || echo "No hardcoded secrets found"
          echo "::endgroup::"
      - name: Check for sensitive data exposure
        run: |
          echo "::group::Data Exposure Check"
          ! grep -rn "console.log\|console.error\|console.warn" src/ --include="*.ts" | grep -i "password\|token\|key\|secret" || echo "No sensitive data in console logs"
          echo "::endgroup::"

  # ── Deploy to VPS (main branch only) ──
  deploy:
    name: "Deploy to VPS"
    runs-on: ubuntu-latest
    needs: [build, security, e2e, accessibility, performance]
    if: github.ref == 'refs/heads/main' && github.event_name == 'push'
    environment:
      name: production
      url: https://void.kluth.cloud
    steps:
      - uses: actions/checkout@v4
      - name: Deploy via SSH
        uses: appleboy/ssh-action@v1
        with:
          host: 72.61.80.234
          username: root
          key: ${{ secrets.VPS_SSH_KEY }}
          script: |
            cd ~/void-runner
            git fetch origin main
            git checkout main
            git pull origin main
            docker compose build frontend
            docker compose up -d --no-deps frontend
            echo "Deployed at $(date)"
      - name: Health check
        run: |
          sleep 10
          STATUS=$(curl -s -o /dev/null -w "%{http_code}" https://void.kluth.cloud)
          if [ "$STATUS" = "200" ]; then
            echo "✅ Site is live (HTTP $STATUS)"
          else
            echo "❌ Site returned HTTP $STATUS"
            exit 1
          fi

  # ── Notify on failure ──
  notify:
    name: "Notify Failures"
    runs-on: ubuntu-latest
    needs: [lint, test, build, e2e, accessibility, performance, security, deploy]
    if: failure()
    steps:
      - name: Report failure
        run: |
          echo "::error::Pipeline failed! Check logs above."
          echo "Failed jobs detected in VOID_RUN CI/CD pipeline."
