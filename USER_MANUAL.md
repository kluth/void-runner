# VOID_RUNNER: Operative's Manual (v3.0 - 1100+ Features)

## 0. Lore: The Great Blackout & The Singularity

In 2039, the "Great Blackout" didn't just turn off the lights; it erased the world's memory. Every centralized database, from bank records to birth certificates, was overwritten with a single string of hex code. Out of this chaos, the **Global Net** was rebuilt—not by governments, but by the **Syndicates**.

You are a **Void Runner**, a neural-linked mercenary operating in the shadows between the corporate nodes. Some say the "Void" is just a glitch in the new world's kernel. Others believe it's a sentient entity born from the data we lost. Whatever it is, it's hungry, it's watching, and it's the only thing that can truly set you free.

---

## 1. System Initialization & The Neural Handshake

Welcome to **VOID_RUNNER**, an immersive, terminal-based cyberpunk hacking simulator. 

### The Strategic Interface Lockdown
The grid enforces absolute identity verification. Standard login vectors are permanently offline. To access the workstation, you must complete the **Google Neural Synchronization**:
- Click `SYNC_VIA_GOOGLE` on the boot screen.
- Your identity will be verified and mapped to a persistent **Legendary Cyber-Alias** (e.g., Neo, Acid Burn) to protect your real name on the public leaderboards.
- Use the `sync` command in the terminal to proactively re-initialize your handshake at any time.

---

## 2. The Terminal Interface

Your primary interaction with the world is through the command-line interface. 

### Essential Commands
- `help` : Display the list of active binaries and system commands.
- `ls` / `dir` : List active mission parameters.
- `whoami` : Display your current operative identity, reputation, and syndicate affiliation.
- `vpm` : Access the **Void Package Manager** (e.g., `vpm list`, `vpm install <package>`).
- `wipe` : Purge your local trace logs (requires the `log-wiper` software).
- `netstat` : View current routing mode (DIRECT, VPN, ONION).
- `ask <prompt>` / `gemini <prompt>` : Communicate with the onboard AI assistant for hints or cryptic lore.
- `dossier` : Request your persistent AI-generated OSINT case file.
- `gallery` : Access your private neural media archive (captured images/sounds).

### Key Metrics
- **Credits (cr):** Used to purchase hardware and software.
- **Data (EXP):** Used to decrypt hardware research and find 0-Days.
- **Reputation (REP):** Determines your standing in the underworld. High reputation unlocks elite contracts and the Global Darknet Node.
- **Trace (Detection):** A percentage indicating how close authorities are to finding your physical location. Dissipates naturally when idle.

---

## 3. Offensive Operations (Deep Interactivity)

Missions are procedurally generated by the **AI Grid Architect**, featuring over 1100+ unique tactical combinations based on real-world tradecraft (OSCP, MITRE ATT&CK). The era of "wait and hope" is over—you must actively execute these hacks through high-fidelity minigames:

- **OSINT Research:** Scrub simulated social media feeds (Bleeter) to extract target passwords.
- **Phishing Campaigns:** Build custom spoofed emails (Sender + Lure) and monitor your live Spam Evasion Score.
- **Buffer Overflow:** Manually manipulate a Hex Memory Grid to overwrite the EIP with JMP/ESP payloads.
- **MITM Attack:** Intercept and decrypt high-speed network packets in real-time.
- **Stock Manipulation:** Time high-frequency trades against a volatile graph linked to **actual real-world asset prices** (BTC, NVDA).
- **Quantum Breach:** Balance unstable qubit waves to prevent mission decoherence.
- **Satellite Hacking:** Synchronize the rotation of orbital relays.

---

## 4. The Tactical Rig Interface

The Black Market has been overhauled into a **Modular Neural Rig**. You don't just "own" hardware; you must physically wire it into your cyberdeck.

### Modular Mounting & Power Management
1. **Purchase/Decrypt:** Acquire modules from the Black Market using Credits or Data.
2. **Mount:** Drag or select a module from `LOCAL_STORAGE` and mount it into one of your 6 expansion slots.
3. **Power Draw (NW):** Every module consumes neural power. Your rig has a maximum capacity of 100 NW. Exceeding this triggers a `POWER_CRITICAL` lockout. Balance your loadout based on the mission at hand.

### Hardware Examples
- **WiFi Pineapple / Flipper Zero:** Boosts Recon stats.
- **USB Rubber Ducky / HackRF One:** Boosts Exploit stats.
- **O.MG Cable / Darknet Router:** Boosts Stealth stats (accelerates natural trace cooldown).

---

## 5. Threats & System Retaliation

If your **Trace reaches 100%**, the Blue Team will strike back.

**Counter-Attacks Include:**
- **NEURAL FEEDBACK:** High-intensity visual distortion that renders your terminal nearly unreadable.
- **BLACK-LISTED:** Locked out of all social channels.
- **CREDIT EXTORTION:** Ransomware locks your funds.
- **KERNEL LOGIC BOMB:** Scrambles your visual interface.

### The Void's Eye (Neural Hijack)
If your **System Integrity** drops too low, an unknown entity will hijack your terminal.
1. The AI will speak directly to you, posing a **riddle or math puzzle**.
2. **Neural Debugger:** After 30 seconds, a hint will appear explaining the puzzle type (Hex, Binary, Math, Word).
3. **FORCE_PURGE:** If you are trapped, after 60 seconds you can expend 250cr to forcefully sever the link and regain control.

---

## 6. The Global Grid & Real-World Sync

**VOID_RUNNER** is a fully persistent multiplayer world hardlinked to physical reality.

### Real-World Neural Bridge
- **Financial Sync:** The grid constantly monitors live prices for BTC, ETH, NVDA, and TSLA, directly affecting the volatility of Stock Manipulation contracts.
- **Cultural Crawl (News):** The procedural news feed incorporates actual tech and security headlines from the last 24 hours.

### Temporal Neural Anchor
- **PATCH_TUESDAY:** This global event (which randomizes ports and increases difficulty) only occurs on actual Tuesdays.
- **WEEKEND_OVERLOAD:** Triggers on Saturdays and Sundays, increasing botnet mining efficiency.

### Syndicates & Comms
- **The Darknet Node:** Locked behind **1000 Reputation**. Access global encrypted comms.
- **Syndicates:** Create or join a team (e.g., CHAOS_COMM_CLUB, F_SOCIETY) for shared chat and passive bonuses.

---

## 7. Deep Workstation Customization

The `set` command allows for granular control over your interface.
- `audio.volume`, `audio.speech`, `audio.ambient`
- `video.matrix`, `video.glitch`, `video.scanlines`, `video.view_mode`
- `social.notifications`, `social.incognito`

---

## 8. Terminal UI (TUI) Mode

For the ultimate hacker experience, VOID_RUNNER can be played directly from your real operating system's terminal, bypassing the browser entirely.

**To initialize the TUI:**
1. Ensure Node.js is installed on your local machine.
2. Open your real terminal and navigate to the game directory.
3. Run `node tui.js`.
4. Type `login <username> <password>` to sync your neural state with the remote server.

---

**Remember:** Leave no trace. Trust no one. The Void is hungry.
